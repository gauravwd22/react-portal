import React from 'react';

function DDCapabilites() {
  return (
    <div className='ddCapabilites'>
      <h1>DD Capabilities</h1>
      <p>
          Data Distributor is a self service that can Consume the data in realtime or batch,
          perform tranformations and ingest the data into various target systems.
          Data distributor leverage’s apache spark as processing engine to run workloads
          faster without the overhead of coding and  deploying for the Consumer.
          Data Distributor also provides the ability to log the key metrics into Kibana.
      </p>



	Some Text 

    </div>
  );
}

export default DDCapabilites;
